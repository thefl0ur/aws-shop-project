info:
  name: "Create product (failed: missing title)"
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/{{path}}"
  body:
    type: json
    data: |-
      {
        "count": 1,
        "price": 1,
        "description": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
