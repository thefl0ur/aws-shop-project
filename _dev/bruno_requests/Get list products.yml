info:
  name: Get list products
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/{{path}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
