[project]
name = "product-service-common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aws-lambda-powertools[parser]>=3.28.0",
    "boto3>=1.43.6",
    "pydantic>=2.0",
]

[tool.uv.sources]
product-service-common = { workspace = true }