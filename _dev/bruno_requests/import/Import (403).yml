info:
  name: Import (403)
  type: http
  seq: 3

http:
  method: GET
  url: "{{import_base_url}}/import?name=foo.csv"
  headers:
    - name: Authorization
      value: Basic bXlfdXNlcjpURVNUX1BBU1NXT1JE
  params:
    - name: name
      value: foo.csv
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
