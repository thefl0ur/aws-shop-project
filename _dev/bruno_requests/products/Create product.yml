info:
  name: Create product
  type: http
  seq: 2

http:
  method: POST
  url: "{{products_base_url}}"
  body:
    type: json
    data: |-
      {
        "count": 2,
        "price": 1,
        "description": "description1",
        "title": "title1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
