info:
  name: "Create product (failed: missing title)"
  type: http
  seq: 1

http:
  method: POST
  url: "{{products_base_url}}"
  body:
    type: json
    data: |-
      {
        "count": 1,
        "price": 1,
        "description": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
