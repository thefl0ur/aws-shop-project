info:
  name: Import (401)
  type: http
  seq: 2

http:
  method: GET
  url: "{{import_base_url}}/import?name=foo.csv"
  params:
    - name: name
      value: foo.csv
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
