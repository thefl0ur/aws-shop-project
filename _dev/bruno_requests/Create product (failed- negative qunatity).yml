info:
  name: "Create product (failed: negative qunatity)"
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/{{path}}"
  body:
    type: json
    data: |-
      {
        "count": -1,
        "price": 1,
        "description": "123",
        "title": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
